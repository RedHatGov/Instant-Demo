---
- name: Provision libvirt VMs
  hosts: localhost
  connection: local
  become: true

  roles:
    - ansible-prvsn-libvirt-vm

- name: Register new VMs and enable repos
  hosts: newvms
  become: true

  roles:
    - ansible-rhsm
